import { newDiv } from "../util/dom";

const toolboxContainer = newDiv({ id: "luogu-toolbox-container" });
const luoguAppContainer = document.getElementById("app");

export { toolboxContainer, luoguAppContainer };
